{
  "name": "saiof-root",
  "version": "1.0.0",
  "description": "SAIOF - Smart AI-Powered Omnichannel Frontend/Backend Foundation",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "concurrently \"npm run start --prefix server\" \"npm run build && npm run preview --prefix client\""
  },
  "keywords": [
    "mern",
    "saiof",
    "ecommerce",
    "boilerplate"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
